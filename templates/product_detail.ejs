<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details - Whop Demo Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <nav class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <h1 class="text-xl font-semibold text-gray-900">Whop Demo Dashboard</h1>
                </div>
                <div class="flex items-center space-x-4">
                    <a href="/products" class="text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">Products</a>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
        <div class="mb-4">
            <a href="/products" class="text-blue-600 hover:underline">← Back to Products</a>
        </div>

        <% if (successMsg) { %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
            <%= successMsg %>
        </div>
        <% } %>

        <% if (errorMsg) { %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
            <%= errorMsg %>
        </div>
        <% } %>

        <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
            <strong>Error:</strong> <%= error %>
        </div>
        <% } %>

        <h2 class="text-2xl font-semibold mb-4">
            <% if (product) { %>
                <%= product.title || product.name || 'Product Details' %>
            <% } else { %>
                Product Details
            <% } %>
        </h2>

        <% if (product) { %>
        <div class="bg-white rounded border p-4 mb-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                    <strong>Product ID:</strong> <%= product.id %>
                </div>
                <div>
                    <strong>Visibility:</strong> <%= product.visibility || 'N/A' %>
                </div>
                <div>
                    <strong>Created:</strong> <%= product.created_at || 'N/A' %>
                </div>
                <div>
                    <strong>Company ID:</strong> <%= product.company_id || 'N/A' %>
                </div>
            </div>
        </div>
        <% } %>

        <!-- Message Sending Section -->
        <div class="bg-white rounded border p-4 mb-6">
            <h3 class="text-lg font-semibold mb-3">Send Message to Members</h3>
            <form method="POST" action="/products/<%= product ? product.id : '' %>">
                <div class="mb-3">
                    <label for="message" class="block text-sm font-medium text-gray-700 mb-1">Message:</label>
                    <textarea 
                        id="message" 
                        name="message" 
                        rows="4" 
                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                        placeholder="Enter your message to send to all members..."
                        required
                    ></textarea>
                </div>
                <button 
                    type="submit" 
                    class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
                >
                    Send Message to <%= memberships ? memberships.length : 0 %> Member(s)
                </button>
            </form>
        </div>

        <!-- Memberships List -->
        <div class="bg-white rounded border">
            <div class="p-4 border-b font-medium">
                Memberships (<%= memberships ? memberships.length : 0 %>)
            </div>
            <div class="overflow-x-auto">
                <table class="min-w-full text-sm">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="text-left px-4 py-2">Membership ID</th>
                            <th class="text-left px-4 py-2">User ID</th>
                            <th class="text-left px-4 py-2">Email</th>
                            <th class="text-left px-4 py-2">Status</th>
                            <th class="text-left px-4 py-2">Valid</th>
                            <th class="text-left px-4 py-2">Created</th>
                            <th class="text-left px-4 py-2">License Key</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (memberships && memberships.length > 0) { %>
                            <% memberships.forEach(function(membership) { %>
                            <tr class="border-t">
                                <td class="px-4 py-2"><%= membership.id || '' %></td>
                                <td class="px-4 py-2"><%= membership.user || '' %></td>
                                <td class="px-4 py-2"><%= membership.email || '' %></td>
                                <td class="px-4 py-2">
                                    <span class="px-2 py-1 text-xs rounded-full 
                                        <% if (membership.status === 'completed') { %>bg-green-100 text-green-800
                                        <% } else if (membership.status === 'pending') { %>bg-yellow-100 text-yellow-800
                                        <% } else { %>bg-gray-100 text-gray-800<% } %>">
                                        <%= membership.status || '' %>
                                    </span>
                                </td>
                                <td class="px-4 py-2">
                                    <% if (membership.valid) { %>
                                        <span class="text-green-600">✓</span>
                                    <% } else { %>
                                        <span class="text-red-600">✗</span>
                                    <% } %>
                                </td>
                                <td class="px-4 py-2"><%= membership.created_at || '' %></td>
                                <td class="px-4 py-2 font-mono text-xs"><%= membership.license_key || '' %></td>
                            </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td class="px-4 py-6 text-center text-gray-500" colspan="7">No memberships found</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <footer class="bg-white border-t mt-12">
        <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 lg:px-8">
            <p class="text-center text-gray-500 text-sm">
                Whop Demo Dashboard - Node.js Server
            </p>
        </div>
    </footer>
</body>
</html>
